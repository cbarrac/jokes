vvavarvar var fvar fsvar fs var fs =var fs = var fs = rvar fs = revar fs = reqvar fs = requvar fs = requivar fs = requirvar fs = requirevar fs = require(var fs = require('var fs = require('fvar fs = require('fsvar fs = require('fs'var fs = require('fs')var fs = require('fs');var fs = require('fs');
var fs = require('fs');
vvar fs = require('fs');
vavar fs = require('fs');
varvar fs = require('fs');
var var fs = require('fs');
var cvar fs = require('fs');
var chvar fs = require('fs');
var chivar fs = require('fs');
var chilvar fs = require('fs');
var childvar fs = require('fs');
var child_var fs = require('fs');
var child_pvar fs = require('fs');
var child_prvar fs = require('fs');
var child_provar fs = require('fs');
var child_procvar fs = require('fs');
var child_procevar fs = require('fs');
var child_procesvar fs = require('fs');
var child_processvar fs = require('fs');
var child_process var fs = require('fs');
var child_process =var fs = require('fs');
var child_process = var fs = require('fs');
var child_process = rvar fs = require('fs');
var child_process = revar fs = require('fs');
var child_process = reqvar fs = require('fs');
var child_process = requvar fs = require('fs');
var child_process = requivar fs = require('fs');
var child_process = requirvar fs = require('fs');
var child_process = requirevar fs = require('fs');
var child_process = require(var fs = require('fs');
var child_process = require('var fs = require('fs');
var child_process = require('cvar fs = require('fs');
var child_process = require('chvar fs = require('fs');
var child_process = require('chivar fs = require('fs');
var child_process = require('chilvar fs = require('fs');
var child_process = require('childvar fs = require('fs');
var child_process = require('child_var fs = require('fs');
var child_process = require('child_pvar fs = require('fs');
var child_process = require('child_prvar fs = require('fs');
var child_process = require('child_provar fs = require('fs');
var child_process = require('child_procvar fs = require('fs');
var child_process = require('child_procevar fs = require('fs');
var child_process = require('child_procesvar fs = require('fs');
var child_process = require('child_processvar fs = require('fs');
var child_process = require('child_process'var fs = require('fs');
var child_process = require('child_process')var fs = require('fs');
var child_process = require('child_process')
var fs = require('fs');
var child_process = require('child_process')

var fs = require('fs');
var child_process = require('child_process')

ivar fs = require('fs');
var child_process = require('child_process')

ifvar fs = require('fs');
var child_process = require('child_process')

if var fs = require('fs');
var child_process = require('child_process')

if (var fs = require('fs');
var child_process = require('child_process')

if ( var fs = require('fs');
var child_process = require('child_process')

if ( pvar fs = require('fs');
var child_process = require('child_process')

if ( prvar fs = require('fs');
var child_process = require('child_process')

if ( provar fs = require('fs');
var child_process = require('child_process')

if ( procvar fs = require('fs');
var child_process = require('child_process')

if ( procevar fs = require('fs');
var child_process = require('child_process')

if ( procesvar fs = require('fs');
var child_process = require('child_process')

if ( processvar fs = require('fs');
var child_process = require('child_process')

if ( process.var fs = require('fs');
var child_process = require('child_process')

if ( process.avar fs = require('fs');
var child_process = require('child_process')

if ( process.arvar fs = require('fs');
var child_process = require('child_process')

if ( process.argvar fs = require('fs');
var child_process = require('child_process')

if ( process.argvvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ]var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] &var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] &&var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && pvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && prvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && provar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && procvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && procevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && procesvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && processvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.avar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.arvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argvvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ]var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] )var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  vvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  vavar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  varvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var ivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var invar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFilvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFilevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile =var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = pvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = prvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = provar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = procvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = procevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = procesvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = processvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.avar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.arvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argvvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  vvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  vavar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  varvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var ovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var ouvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFilvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFilevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile =var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = pvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = prvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = provar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = procvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = procevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = procesvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = processvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.avar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.arvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argvvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  cvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  covar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  convar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  consvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  consovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  consolvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  consolevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.ivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.invar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.infvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.infovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info(var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Wvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Wrvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Wrivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writinvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writingvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing fvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing frvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing frovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing fromvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %svar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s tvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s tovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %svar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s"var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s",var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", ivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", invar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFilvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFilevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile,var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, ovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, ouvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFilvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFilevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  vvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  vavar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  varvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var ovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var ouvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFDvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD =var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fsvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.ovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.opvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.opevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSyvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSynvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSyncvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(ovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(ouvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFilvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFilevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile,var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'wvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w'var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fsvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.rvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.revar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.reavar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFilvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFilevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(ivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(invar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFilvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFilevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile,var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, fvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, fuvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, funvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, funcvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, functvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, functivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, functiovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, functionvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(evar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(ervar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(errvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,dvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,davar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,datvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,datavar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data)var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
  var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
   var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    vvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    vavar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    varvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var lvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var levar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var lenvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var lengvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var lengtvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var lengthvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length =var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = dvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = davar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = datvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = datavar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.lvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.levar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.lenvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.lengvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.lengtvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.lengthvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
  var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
   var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    cvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    covar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    convar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    consvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    consovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    consolvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    consolevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.ivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.invar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.infvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.infovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info(var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Byvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytesvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes:var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %svar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s"var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s",var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", lvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", levar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", lenvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", lengvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", lengtvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", lengthvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
  var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
   var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    cvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    chvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    chivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    chilvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    childvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_pvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_prvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_provar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_procvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_procevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_procesvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_processvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.evar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.exvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.exevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFilvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFilevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSyvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSynvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSyncvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync(var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/uvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usrvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/binvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/gvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/givar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/gitvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git'var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git',var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', [var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['avar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['advar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['addvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add'var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add',var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', ovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', ouvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFilvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFilevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile]var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
  var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
   var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    vvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    vavar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    varvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var avar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var arvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var argvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var argsvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args =var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = [var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['cvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['covar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['comvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commitvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit'var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit',var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', ovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', ouvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFilvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFilevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile,var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, 'var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-mvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m'var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m',var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Uvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Upvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Updvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Updavar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Updatvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Updatevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update cvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update chvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update chavar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update charvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update charavar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update characvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update charactvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update charactevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update charactervar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character'var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
  var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
   var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    fvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    fovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    forvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (vvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (vavar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (varvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var cvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var covar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var couvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var countvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var countevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var countervar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter =var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1;var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; cvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; covar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; couvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; countvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; countevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; countervar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <=var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= lvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= levar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= lenvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= lengvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= lengtvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= lengthvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length;var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; cvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; covar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; couvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; countvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; countevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; countervar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter+var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
  var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
   var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
 var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
  var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
   var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
    var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
     var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fsvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.wvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.wrvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.wrivar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writevar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSyvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSynvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSyncvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(ovar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(ouvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFDvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD,var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, dvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, davar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, datvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, datavar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, data,var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, data, var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, data, 0var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, data, 0,var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, data, 0, var fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, data, 0, cvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, data, 0, covar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, data, 0, couvar fs = require('fs');
var child_process = require('child_process')

if ( process.argv[ 2 ] && process.argv[ 3 ] ) {
  var inFile = process.argv[ 2 ]
  var outFile = process.argv[ 3 ]
  console.info("Writing from %s to %s", inFile, outFile)
  var outFD = fs.openSync(outFile, 'w')
  fs.readFile(inFile, function(err,data) {
    var length = data.length
    console.info("Bytes: %s", length)
    child_process.execFileSync('/usr/bin/git', ['add', outFile])
    var args = ['commit', outFile, '-m', 'Update character']
    for (var counter = 1; counter <= length; counter++)
    {
      fs.writeSync(outFD, data, 0, coun